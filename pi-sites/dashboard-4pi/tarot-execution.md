# Execution Plan

## Tasks
- [x] Task 1: UI primitives — Polish Avatar (image support), Modal (Escape key, animation, portal), Button/Input focus-visible
- [x] Task 2: Theme system — Fix Tailwind v4 dark variant to use data-theme, verify ThemeProvider/ThemeToggle/CSS vars end-to-end
- [x] Task 3: Auth flow — Verify login/signup/reset/ProtectedRoute/RoleGate work end-to-end, add demo credential autofill, redirect-after-login
- [x] Task 4: Layout — AppShell, Sidebar (collapsible), Header (menu + theme toggle + user avatar + logout) — AppShell, Sidebar, Header (theme toggle + user avatar + logout)
- [x] Task 5: Kanban — Wire @dnd-kit drag-and-drop, TaskModal CRUD (create+edit), DeleteConfirmDialog, DragOverlay, feed events on all actions — Wire @dnd-kit drag-and-drop, TaskModal CRUD, DeleteConfirmDialog
- [x] Task 6: Activity Feed — FeedStore events, FeedItems with icon bubbles, auto-scroll with user-scroll detection, enriched feed simulator, animated entries — FeedStore, FeedItems, auto-scroll, simulated events via feedSimulator
- [x] Task 7: Analytics — Connected Recharts to real taskStore data via shared analyticsStore + useFilteredTasks hook, date range filtering, theme-aware tooltip/chart colors, fixed PriorityBreakdownChart (single Bar with per-Cell fills using violet/blue/amber/red), added summary stat cards — Connect Recharts to taskStore data, date filtering, theme-aware colors, fix PriorityBreakdownChart colors
- [x] Task 8: Tarot Club Panel — ProjectWidget with live task count + progress bar, MemberCards with phase badges, ReflectionCards with member icons + pill labels, TokenUsageTracker with progress bars + bar chart — ProjectWidget, TeamGrid, MemberCards, Reflections, TokenUsageTracker
- [x] Task 9: Polish — Dashboard with stat cards + progress bar + recent tasks + tech badges, Settings with theme toggle + sign out, responsive grid layouts, transitions — Transitions, responsive tweaks, edge cases
